export const reyStaking = [
  // Read Only functions
  "function availableRewards(address _staker) view returns (uint256)",
  // Write Only functions
  "function stake(uint256 tokenId) nonpayable returns (uint256)",
  "function withdraw(uint256 tokenId) nonpayable returns (uint256)"
];
